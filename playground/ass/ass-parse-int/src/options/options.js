import React, { Component } from 'react';
import './options.css';

class Options extends Component {
  render() {
    return (
      <div className="Options">
        <button type="button" className="btn btn-info">生成</button>
      </div>
    );
  }
}

export default Options;
